/**
 * Intentionally empty.
 *
 * Agent/model execution is local to `packages/cli`. The cloud server no longer
 * exposes a chat execution endpoint; it only persists session state and hosts
 * account-related cloud APIs.
 */
export {};
